# relative path for code generation
target:
  relativeDir:
    project:
      - ""
    proto:
      - "pkg"
      - "{SERVICE_NAME}"
    client:
      - "pkg"
      - "{SERVICE_NAME}"
    server:
      - "services"
      - "{SERVICE_NAME}server"
template:
  filesFormatSuffix: ".tmpl"
  protoFilename: "{SERVICE_NAME}.proto.tmpl"
  relativeDir:
    project:
      - "project"
    proto:
      - "proto"
    client:
      - "client"
    server:
      - "server"

