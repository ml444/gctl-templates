package app

// struct field
const (
{{- range $i, $field := .ModelFieldList }}
	Field{{$field | ToCamelCase}} = "{{$field | ToCamelCase}}"
{{- end }}
)

// db field
const (
{{- range $i, $field := .ModelFieldList }}
	dbField{{$field | ToCamelCase}} = "{{$field}}"
{{- end }}
)
