target:
  relativeDir:   # relative path for code generation
    proto:
    - "{SERVICE_NAME}Server"
    - "api"
    client:
    - "{SERVICE_NAME}Server"
    - "{SERVICE_NAME}"
    server:
    - "{SERVICE_NAME}Server"
template:
  filesFormatSuffix: ".tmpl"
  protoFilename: "{SERVICE_NAME}.proto.tmpl"
  relativeDir:   # relative path for template
    proto:
    - "proto_template"
    client:
    - "client_template"
    server:
    - "SERVICE"


